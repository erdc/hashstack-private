extends: [base_package]
dependencies:
  build: []

sources:
  - url: http://homepages.rpi.edu/~zhanga/SCOREC-1.2.0-Linux.tar.gz
    key: tar.gz:nlxajbaiqfiofug2bgzivtseri3wnml7

build_stages:

- name: install
  after: prologue
  handler: bash
  bash: |
    cp -r include ${ARTIFACT}/include
    cp -r lib ${ARTIFACT}/lib

