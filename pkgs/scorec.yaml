extends: [base_package]
dependencies:
  build: [mpi, parmetis, zoltan, simmetrix]

sources:
  - url: http://www.scorec.rpi.edu/~dibanez/SCOREC-1.0.0-Linux.tar.gz
    key: tar.gz:6gkv3a7uyqjcweognjnh47vblr7m5sku

build_stages:

- name: install
  after: prologue
  handler: bash
  bash: |
    cp -r include ${ARTIFACT}/include
    cp -r lib ${ARTIFACT}/lib

