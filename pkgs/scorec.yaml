extends: [base_package]
dependencies:
  build: []

sources:
  - url: http://www.scorec.rpi.edu/~dibanez/SCOREC-1.2.0-Linux.tar.gz
    key: tar.gz:pejcazrou7tlmygjt65juxy3m3eiyuwo
    
#defaults:
#  relocatable: true

defaults:
  relocatable: false
>>>>>>> origin/scorec

build_stages:

- name: install
  after: prologue
  handler: bash
  bash: |
    cp -r include ${ARTIFACT}/include
    cp -r lib ${ARTIFACT}/lib

