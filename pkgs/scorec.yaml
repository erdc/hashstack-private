extends: [base_package]
dependencies:
  build: []

sources:
  - url: http://www.scorec.rpi.edu/~dibanez/SCOREC-1.0.2-Linux.tar.gz
    key: tar.gz:eskfopgzawjb4dmisp4qvdi6ddxnpcll

build_stages:

- name: install
  after: prologue
  handler: bash
  bash: |
    cp -r include ${ARTIFACT}/include
    cp -r lib ${ARTIFACT}/lib
    cp -r bin ${ARTIFACT}/bin

